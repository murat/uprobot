<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Uprobot</title>

    <link rel="stylesheet" href='<%= Routes.static_path(@conn, "/css/app.css") %>'/>
  </head>
  <body>
    <nav class="navbar" role="navigation" aria-label="main navigation">
      <div class="container" style="padding: 0 1rem;">
        <div class="navbar-brand">
          <a class="navbar-item" href='<%= Routes.page_path(@conn, :index) %>'>
            <img src='<%= Routes.static_path(@conn, "/images/logo.jpg") %>' />
          </a>

          <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
          </a>
        </div>

        <div id="navbarBasicExample" class="navbar-menu">
          <%= if @user_signed_in? do %>
            <div class="navbar-start">
              <a class="navbar-item" href='<%= Routes.site_path(@conn, :index) %>'>
                Sites
              </a>
            </div>

            <div class="navbar-end">
              <div class="navbar-item">
                <div class="buttons">
                  <%= link("Sign out", to: Routes.session_path(@conn, :delete), method: :delete, class: "button is-dark") %>
                </div>
              </div>
            </div>
          <% else %>
            <div class="navbar-end">
              <div class="navbar-item">
                <div class="buttons">
                  <%= link("Sign up", to: Routes.auth_path(@conn, :new), class: "button is-primary") %>
                  <%= link("Sign in", to: Routes.session_path(@conn, :new), class: "button is-light") %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="container" style="margin-top: 2rem; padding: 0 1rem;">
      <%= unless is_nil(get_flash(@conn, :info)) do %>
        <p class="notification is-info"><%= get_flash(@conn, :info) %></p>
      <% end %>
      <%= unless is_nil(get_flash(@conn, :error)) do %>
        <p class="notification is-danger"><%= get_flash(@conn, :error) %></p>
      <% end %>

      <%= render @view_module, @view_template, assigns %>
    </div>

    <script type="text/javascript" src='<%= Routes.static_path(@conn, "/js/app.js") %>'></script>
    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
  </body>
</html>
